---
import { getEntry, render } from 'astro:content'
import MagicLink from '@/components/content/magic-link.astro'
import ProseLayout from '@/components/content/prose-layout.astro'
import RubyAnnotation from '@/components/content/ruby-annotation.astro'
import GithubStatsIsland from '@/components/showcase/github-stats-island.astro'
import BaseLayout from '@/layouts/base-layout.astro'

const about = (await getEntry('pages', 'about'))!

const { Content } = await render(about)
---

<BaseLayout title={about.data.title || 'About - octohash'}>
  <ProseLayout frontmatter={about.data}>
    <Content components={{ GithubStats: GithubStatsIsland, MagicLink, RubyAnnotation }} />
  </ProseLayout>
</BaseLayout>
